# Required GitHub secrets:
#   SERVER_IP       — VPS public IP address
#   SERVER_USER     — SSH username on the server (e.g. deploy)
#   SSH_PRIVATE_KEY — Private SSH key (public key must be in ~/.ssh/authorized_keys on server)
#   SERVER_PORT     — SSH port (usually 22)
#   APP_DOMAIN      — e.g. stage9.duckdns.org

name: CD

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          cat > ~/.ssh/id_rsa << 'EOF'
          ${{ secrets.SSH_PRIVATE_KEY }}
          EOF
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p ${{ secrets.SERVER_PORT }} ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Test SSH connectivity
        run: |
          ssh -v -p ${{ secrets.SERVER_PORT }} \
            -o StrictHostKeyChecking=accept-new \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} \
            echo "SSH connection successful"

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            set -e
            mkdir -p /var/www/scheduler-api
            cd /var/www/scheduler-api
            
            # Initialize repository if it doesn't exist
            if [ ! -d .git ]; then
              git clone https://github.com/amiabl-programr/stage-9-background-job-scheduler-be.git .
            fi
            
            # Pull latest changes
            git fetch origin main
            git reset --hard origin/main
            
            # Install and build
            pnpm install --frozen-lockfile
            pnpm build
            pnpm migration:run
            
            # Reload application
            pm2 reload ecosystem.config.js --update-env

      - name: Health check
        run: |
          curl --fail --retry 5 --retry-delay 5 --retry-connrefused \
            https://${{ secrets.APP_DOMAIN }}/api/health

      - name: Write deployment summary
        run: |
          echo "## Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Time (UTC):** $(date -u '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          echo "- **Health check:** ✅ passed" >> $GITHUB_STEP_SUMMARY
